class TraverseFilesystem:
'this class is for traversing the filesystem/folder to find all archives of a certain type'

   def __init__(self, recursive):
      self.path = name
      self.recursive = recursive
      self.obj = {}
   
   def traverseAllFilesFromPath(self, startingPath, FileType):
     # get all archives of type FileType, store in a data structure
     # fill the path

   def getTotalBytesFromPath(self):
      print "Name : ", self.name,  ", Salary: ", self.salary
